# ESP32 AFSK Test Harness

This example builds the serial test harness firmware that was previously in `src/main.cpp`.

Build:
- `pio run -d examples/basic/harness`

Upload:
- `pio run -d examples/basic/harness -t upload`
